class Book {

    constructor(title, author, type) {
      this.title = title;
      this.author = author;
      this.type = type; // 'novel', 'biography', 'non-fiction'
      this.id = null;
      this.read = false;
      this.pages = null;
    }
  
  }